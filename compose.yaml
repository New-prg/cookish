name: listok

services:
  listok:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: listok-purchases
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - path: .env
        required: false
    environment:
      NODE_ENV: production
      PORT: 3000
